---
phase: 09
plan: 04
title: Activity Mode Rotation + Integration
type: tdd
requirements: [NUM-04, NUM-05, NUM-07]
depends_on: [09-03]
---

# Plan 09-04: Tölur Activity Mode Rotation

Per CONTEXT D-15, simplify Tölur to 2 modes (`TapToHear` / `Activity`).
The Activity mode picks one of {Sequence, Correspondence, Subitizing,
Addition} per round (random rotation).

## Tasks

1. RED: tests for `TolurMode` change (sequence → activity).
2. GREEN: refactor TolurMode + widget rotation (`ActivityRotator`).
3. RED: integration test exercising the rotation in TolurRoom.
4. GREEN: integration glue.

## Done criteria

- `TolurMode` enum has exactly 2 values: `tapToHear`, `activity`.
- Activity mode renders an ActivityRotator that cycles through the 4
  activities by round (random selection).
- Integration test: toggle to activity → cycle through ≥3 activity
  variants without crashes.
