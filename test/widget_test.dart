// Aggregator placeholder. The real widget tests live under
// test/features/home/home_page_test.dart and test/app/app_test.dart.
// `flutter create` produced a counter-app boilerplate test that referred to a
// `MyApp` class that no longer exists; we replace it with this comment so the
// test runner does not pick up an orphan.
